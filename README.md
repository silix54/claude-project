# claude-project

Personal function suite — a collection of standalone tools, each in its own
subdirectory.

## Contents

- [`silas-ops/`](silas-ops/) — personal operations dashboard (Flask + htmx).
  Daily view, Sunday planning, habit/mood reflection, journal, training
  periodization. See `silas-ops/README.md`.
